import { ZenithShell } from '@/components/zenith-shell';
export default function GitHubPage(){return <ZenithShell><div className="page"><div className="eyebrow">GitHub evidence</div><h1 style={{fontSize:36,letterSpacing:'-.05em'}}>Your engineering history.</h1><p className="muted">Connect a repository when OAuth is configured. Verified commits can enrich project history, but a commit alone never proves mastery.</p><div className="card" style={{marginTop:25}}><span className="tag">READY FOR OAUTH</span><h3>Repository connection</h3><p>OAuth, repository selection, branches, tags, history and synchronization are designed behind the integration boundary.</p></div></div></ZenithShell>}
