'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
const domains=['Web','Systems','AI / ML','Security','Cloud / DevOps','Databases','Graphics','Robotics'];
export default function Onboarding(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [experience,setExperience]=useState('absolute-zero');const [selected,setSelected]=useState<string[]>(['Web']);const [error,setError]=useState('');const [busy,setBusy]=useState(false);function toggle(d:string){setSelected(s=>s.includes(d)?s.filter(x=>x!==d):[...s,d])}async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,password,programmingExperience:experience,domains:selected})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Registration failed');location.href='/dashboard'}catch(e){setError(e instanceof Error?e.message:'Registration failed')}finally{setBusy(false)}}return <main className="auth-wrap"><div className="auth" style={{width:'min(620px,100%)'}}><Link className="brand" href="/"><span className="mark">Z</span><span>ZENITH LEARNING</span></Link><div className="eyebrow" style={{marginTop:25}}>Adaptive onboarding</div><h1>Start from where you actually are.</h1><p className="muted">Zenith uses this information to choose a sensible anchor and avoid teaching what you already know.</p><form onSubmit={submit}><div className="field"><label>Name</label><input required value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div><div className="field"><label>Programming experience</label><select value={experience} onChange={e=>setExperience(e.target.value)}><option value="absolute-zero">I don't know yet / absolute zero</option><option value="beginner">Beginner</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option><option value="expert">Expert</option></select></div><div className="field"><label>Domains you want to explore</label><div style={{display:'flex',gap:7,flexWrap:'wrap'}}>{domains.map(d=><button type="button" key={d} className={'btn '+(selected.includes(d)?'primary':'')} onClick={()=>toggle(d)}>{d}</button>)}</div></div>{error&&<div style={{color:'var(--danger)',fontSize:13,margin:'12px 0'}}>{error}</div>}<button className="btn primary" disabled={busy}>{busy?'Creating your path…':'Create account →'}</button></form><p className="muted" style={{fontSize:12,marginTop:16}}>Required backend: DATABASE_URL. Passwords are hashed server-side; secrets never enter the client.</p></div></main>}
