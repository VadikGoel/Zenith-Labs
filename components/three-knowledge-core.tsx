'use client';
import { useEffect, useRef } from 'react';
export function ThreeKnowledgeCore(){const host=useRef<HTMLDivElement>(null);useEffect(()=>{let renderer:any;let frame=0;let scene:any;let camera:any;let geometry:any;let material:any;let mesh:any;import('three').then(THREE=>{if(!host.current)return;scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(45,1,.1,100);camera.position.z=4;renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(host.current.clientWidth,host.current.clientHeight);host.current.appendChild(renderer.domElement);geometry=new THREE.IcosahedronGeometry(1.15,1);material=new THREE.MeshBasicMaterial({wireframe:true,color:0xd8ff5b,transparent:true,opacity:.55});mesh=new THREE.Mesh(geometry,material);scene.add(mesh);const loop=()=>{frame=requestAnimationFrame(loop);mesh.rotation.x+=.002;mesh.rotation.y+=.004;renderer.render(scene,camera)};loop();const resize=()=>{if(!host.current)return;const w=host.current.clientWidth,h=host.current.clientHeight;camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h)};window.addEventListener('resize',resize)});return()=>{cancelAnimationFrame(frame);window.removeEventListener('resize',()=>{});geometry?.dispose();material?.dispose();renderer?.dispose();renderer?.domElement?.remove() }},[]);return <div ref={host} style={{position:'absolute',right:20,bottom:20,width:190,height:190,pointerEvents:'none'}} aria-hidden="true"/>}
